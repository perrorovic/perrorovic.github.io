{
  "name": "perrorovic.github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf _site && eleventy && (docker compose restart website 2>/dev/null || true)",
    "serve": "eleventy --serve --port=3000",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
