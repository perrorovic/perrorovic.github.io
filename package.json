{
  "name": "perrorovic.github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "music": "node scripts/fetch-music.js",
    "build": "rm -rf _site && npm run music && eleventy && (docker compose restart website 2>/dev/null || true)",
    "serve": "eleventy --serve --port=3000",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
